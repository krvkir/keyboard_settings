xkb_keymap {
    xkb_keycodes  { include "evdev+aliases(qwerty)"	};
    xkb_types     { include "complete"	};
    xkb_compat    { include "complete"	};
    xkb_symbols   {
        include "pc+us+ru(winkeys):2+inet(evdev)"
        include "pc(fnkeys)"

        // include "pc(level3_caps_switch)"
        include "pc(overlay1_caps)"

        include "group(shifts_toggle)"
        // include "group(ctrl_shift_toggle)"
        // include "group(alt_shift_toggle)"

        // include "pc(level3_win_switch)"
    };
    xkb_geometry  { include "custom(thinkpad_s1)"	};
};
